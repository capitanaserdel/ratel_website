import React from 'react';

export const metadata = {
  title: "Fiber Broadband Services & Plans | Ratel Plus",
  description: "Explore Ratel Plus high-speed Fibre to the Home (FTTH) broadband pricing plans starting at ₦15,000/month. Unlimited bandwidth and symmetric speeds.",
};

export default function Layout({ children }) {
  return children;
}
